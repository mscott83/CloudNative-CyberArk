# Scripts for cloud-init and cloud-config

## Proxmox Config

Follow these steps to build a base image with SIA trust included.

1. Get the cloud image you want to use for your build.
```
    wget https://cloud-images.ubuntu.com/resolute/current/resolute-server-cloudimg-amd64.img
```